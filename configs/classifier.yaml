source: configured_dataset
dataset_config: configs/dataset.yaml
model_variant: embedding_mlp     #mlp/embedding_mlp/attention_tabular
batch_size: 512
epochs: 20
lr: 0.001
weight_decay: 0.0001
hidden_dim: 256
dropout: 0.3
embedding_dim: 16
numeric_projection_dim: 64
attention_dim: 128
attention_heads: 8
attention_layers: 3
patience: 4
num_workers: 0
random_state: 42
output_dir: outputs/classifier
